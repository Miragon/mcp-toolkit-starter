# Port the MCP server listens on (read by mcp-use itself).
PORT=3010
# Public origin override for deployed servers (read by mcp-use itself);
# locally the origin is resolved from the request — leave it unset.
# MCP_URL=https://my-mcp-server.example.com
